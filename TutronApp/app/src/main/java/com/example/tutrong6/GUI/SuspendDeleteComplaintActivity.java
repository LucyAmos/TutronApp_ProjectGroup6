package com.example.tutrong6.GUI;

import android.text.TextUtils;
import android.view.LayoutInflater;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

import androidx.appcompat.app.AlertDialog;
/*
public class SuspendDeleteComplaintActivity( String TutorName, String DateSuspension) {


            I started the codebut its not finish

            The tutor and date suspension need to be fixed in the UI
                AlertDialog.Builder dialogBuilder = new AlertDialog.Builder(this);
            LayoutInflater inflater = getLayoutInflater();
            final View dialogView = inflater.inflate(R.layout.update_dialog, null);
            dialogBuilder.setView(dialogView);

            final TextView  TextTutorName = dialogView.findViewById(R.id.tutorname);

            final TextView TextDateSuspension = dialogView.findViewById(R.id.date_suspension);
            final Button buttonTemporarySuspension = dialogView.findViewById(R.id.buttonTemporarySuspension);
            final Button buttonDelete = dialogView.findViewById(R.id.buttonDismiss);

            dialogBuilder.setTitle(productName);
            final AlertDialog b = dialogBuilder.create();
            b.show();




}

        */
